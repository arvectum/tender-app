# HANDOVER — tender-small (fresh)

Дата: 2026-05-24 13:23:08 +0300  
Репозиторий: `/Users/master/tender-app`  
Ветка: `main`  
Head: `cbbd8d059a3767437e309c4c7fcd42a140399c56`

## 1) Цель текущего этапа

Собрать **жёсткий, воспроизводимый** пайплайн расчёта участия в малых закупках, где в финал попадают только:
- закупки на **поставку товаров**,
- с **извлечённым/учтённым текстом ТЗ** (особенно для `mos_portal`),
- со **strict/full match** по предмету,
- с **реальным источником рыночной цены** (`offer_source_url`),
- с **низким/контролируемым риском маржи**.

## 2) Что уже сделано (по коду и артефактам)

- Усилена fail-closed логика price-search и фильтрация procurement-доменов как рыночных источников.
- Добавлены fallback-сценарии для `mos_portal` в части извлечения данных/ТЗ.
- Обновлены тесты под новую логику strict/full-match и source selection.
- Есть серия артефактов fullscan (`v1..v8`) в `exports/`.

Ключевые последние коммиты:
- `cbbd8d0` Harden vendor retrieval fallback and block zakupki360 procurement domains
- `5355045` Add multi-query vendor retrieval fallback for yandex price search
- `93f5aca` Improve vendor-source retrieval and full-match gating for procurement domains

## 3) Текущие проблемы / риски

- Рабочее дерево сильно зашумлено untracked-артефактами (`exports/`, `diagnostics/`, `data/backup`) — риск потери воспроизводимости и контроля версий.
- Часть кейсов всё ещё даёт `tz_extraction_failed` / `rejected_non_strict_match` в диагностике.
- Нужен стабильный operational runbook для регулярного прогона и контроля качества результата.

## 4) Что делаем дальше (P0)

1. Зафиксировать policy по артефактам (что хранится в git, что — вне git).  
2. Прогонять pipeline только в режиме жёсткого отбора:
   - исключение работ/услуг,
   - обязательный `offer_source_url`,
   - приоритизация low-margin релевантных full-match кейсов.
3. Отдельно дожать extraction ТЗ по `mos_portal` (вложения + fallback-источники), чтобы сократить `tz_extraction_failed`.
4. Сформировать «операционный отчёт» одного формата: shortlist + diagnostics + причины исключения.

## 5) Definition of Done для целевого результата

Считаем этап успешным, когда на выходе каждого прогона есть:
- `shortlist` только по товарам (без работ/услуг),
- у каждой позиции есть подтверждённый `offer_source_url`,
- все кандидаты full-match (или явно отфильтрованы с причиной),
- маржа в адекватной зоне риска (без ставок на >50%, осторожно с >25%),
- diagnostics прозрачно объясняет каждое включение/исключение.

## 6) Команда продолжения (оператору/следующему агенту)

- Не расширять охват до новых источников, пока не стабилизирован quality baseline по `mos_portal`.
- Любое послабление фильтров strict/full-match считать регрессом.
- Любой «красивый» результат без `offer_source_url` считать недействительным.
